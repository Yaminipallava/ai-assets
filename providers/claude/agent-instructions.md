# Claude Agent Instructions

Apply the shared repo conventions first.

Claude-specific notes should live here only when they differ from the generic core, for example:
- message structuring conventions
- memory-file usage
- provider-specific approval or tool behavior
