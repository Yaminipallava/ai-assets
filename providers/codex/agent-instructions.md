# Codex Agent Instructions

Apply the shared repo conventions first.

Codex-specific notes belong here only when they depend on:
- tool-calling behavior
- approval flow
- session memory or instruction-file behavior
