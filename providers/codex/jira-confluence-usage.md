# Codex Jira and Confluence Usage

Use the generic Jira and Confluence command/workflow docs as the default.

Add Codex-specific notes here only when:
- tool approval behavior matters
- command execution constraints matter
- session-local context affects how updates are staged
