# Jira Env Pattern

Recommended local pattern:

```bash
source ~/.config/nunneri/jira.env
```

Typical contents:
- `JIRA_BASE_URL`
- `JIRA_EMAIL`
- `JIRA_API_TOKEN`
- `JIRA_PROJECT_KEY`

Keep the env file:
- outside the repo
- machine-local
- uncommitted
