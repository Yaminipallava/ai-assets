# Commands

This folder holds reusable command patterns.

Put here:
- Git branch and cleanup commands
- Jira REST command shapes
- Confluence REST command shapes
- validation command patterns

Keep commands:
- copyable
- minimal
- generic by default

Project-specific command examples belong under `projects/`.
